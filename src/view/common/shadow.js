import {em, hm} from './const'

const shadow=(elevation,color)=>{
    const shadowProps = [[16,24],[23,25],[3,6],[9,16]]
 return {
     shadowOpacity:1,
     shadowColor:color,
     shadowRadius:
     shadowOffset:{width:0, height:}
 }   
}